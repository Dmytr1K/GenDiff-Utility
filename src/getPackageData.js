// TODO: fix get data from package.json

// import fs from 'fs';

// const packageJson = fs.readFileSync('./package.json');

// export const { version, description } = JSON.parse(packageJson);

export const version = '0.0.1';
export const description = 'Compares two configuration files and shows a difference.';
